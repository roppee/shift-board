# CLAUDE.md — shift-board プロジェクト

## プロジェクト概要

ヘルパーステーション欅のシフト管理アプリ。**単一の HTML ファイル（Shift.html）** で完結しており、ビルドツール・パッケージマネージャは一切使用しない。

- React 18 / ReactDOM（CDN）
- Babel Standalone（JSX をブラウザ内でトランスパイル）
- Google Fonts（Noto Sans JP）
- データ永続化: ブラウザの `localStorage`（キー: `shift_app_v1`）

---

## ファイル構成

```
shift-board/
├── Shift.html   # アプリ本体（全コードがここに集約）
├── README.md
└── Shift icon.png
```

JSX コードは `<script id="app-src" type="text/plain">` タグ内に記述され、ページロード時に Babel がトランスパイルして実行する。

---

## 機能一覧

### タブ

| タブ名 | 概要 |
|--------|------|
| 勤務表 | 月表示・週表示の切替。セルクリックでシフト変更、長押しでセルをロック。行単位の一括ロック/解除。前月末尾を薄く表示（月をまたぐ連続勤務の確認用）。 |
| 個人ビュー | スタッフ1名ずつカレンダー形式で月間シフトを表示・編集。勤務日数・時間・希望休件数のサマリ付き。 |
| 希望休 | スタッフ × 日付マトリクスで希望休を登録・管理。月切替可。 |
| ⚠ アラート | 「人員不足」と「個人制約違反」を一覧表示。違反理由を具体的なテキストで表示。 |
| 個人制約 | スタッフごとに最小/最大勤務日数・時間・最大連続勤務日・出勤不可曜日・担当可能シフト・メモを設定。 |
| 必要人数 | シフト種別の追加・編集・削除と必要人数設定。固定ルール（特定スタッフ×曜日×時間帯）。シフト順序制約テーブル（例: 夜勤翌日に早番不可）。 |
| 従業員管理 | スタッフの追加・編集（氏名・役職・性別）・削除。当月の勤務実績をインライン表示。 |

### ヘッダーボタン

| ボタン | 動作 |
|--------|------|
| ⚡ 自動生成 | 制約・希望休・固定ルール・ロックセルを考慮して1パターンを即時生成 |
| 🎲 候補生成 | 複数パターン（3/5/10件選択可）を生成しアラート数の少ない順に比較・採用 |
| ✅ 確定して翌月へ | 当月シフトを `savedMonths` に保存し翌月へ移動 |
| 🖨 プレビュー | A4横向き・全スタッフ一覧表を別ウィンドウで印刷プレビュー表示 |
| 📅 カレンダー | スタッフ1名1ページのカレンダー形式 PDF 出力 |
| 📤 保存 | 全データを JSON ファイルにエクスポート |
| 📥 読込 | JSON ファイルからインポート（全状態を上書き復元） |

---

## データ構造の要点

```
staffShifts[empId][day]  // day は 1〜31 の数値
wishdays[empId]          // Set<dateKey>  dateKey = "YYYY-MM-DD"
lockedCells[empId]       // Set<day>
constraints[empId]       // 個人制約オブジェクト
savedMonths["YYYY-M"]    // 確定済み月のシフトスナップショット
fixedRules[]             // 固定ルール配列
```

- `Set` は localStorage 保存時に配列へ変換し、読み込み時に `Set` へ復元する。
- `endH > 24` は翌日にまたぐシフトを意味する（例: `endH: 29` → 翌 05:00）。
- 休日は `"休み"` / `"希望休"` の文字列で管理。それ以外はカスタムシフトのキー文字列。

---

## 開発上の注意点

### 1. 単一ファイル制約
すべてのコード（HTML / CSS / JS / JSX）が `Shift.html` 1ファイルに収まっている。分割・モジュール化は行わない。外部 npm パッケージを追加するには CDN を使う。

### 2. ビルド不要・トランスパイルはブラウザ任せ
`babel.min.js` がブラウザ内でリアルタイムにトランスパイルする。TypeScript・ESM import は使用不可。`type="text/babel"` ではなく `type="text/plain"` で JSX を記述し、ページロード後にスクリプトで動的に実行している。

### 3. スタイル定数は App コンポーネント外で定義する
`C`（カラー）と `S`（スタイル関数）は App の外に定義されており、再レンダリングのたびに再生成されない。新しいスタイル定数を追加するときも App 外に置くこと。

### 4. 自動生成はランダム性を持つ
`autoGenerate` 関数は `Math.random()` を使ったシャッフルを含む。候補生成（🎲）は複数回呼び出してアラート数で比較するため、結果は毎回異なる。

### 5. 月をまたぐ連続勤務
前月データは `savedMonths` から取得した `prevMonthTail` として自動生成エンジンと制約チェックに渡される。月を確定（✅）しないと翌月の連続勤務チェックが機能しない。

### 6. シフトキーはラベルと同一
シフトの `key` と `label` は同じ文字列。名称を変更すると `required`・`orderConstraints`・各スタッフの `preferredShifts` の参照も一括更新が必要になる（`saveEdit` 関数内で実施済み）。

### 7. 固定ルールは customShifts にも追加される
固定ルールを登録すると、同じ時間帯の既存シフトが存在しない場合に `isFixedRule: true` フラグ付きで `customShifts` にも追加される。削除時は両方から取り除くこと。

### 8. 印刷・PDF 出力は別ウィンドウ
`exportPDF` / `exportCalendarPDF` は `window.open` で新しいウィンドウを開き、HTML 文字列を直接 `document.write` する。React の外側で生成されるため、状態変化の影響を受けない。

### 9. localStorage の容量
スタッフ数・月数が増えると `localStorage` の容量（通常 5MB）を超える可能性がある。長期運用時は定期的に JSON エクスポートしてバックアップを取ること。
## 過去の失敗と教訓

### ファイル名の大文字・小文字
- GitHub Pagesはファイル名の大文字小文字を区別する
- HTMLに書いたファイル名と完全一致しているか必ず確認する
- 例：icon.PNG と icon.png は別物として扱われる